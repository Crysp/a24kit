import palette from './palette';

const theme = {
    baseFontSize: 16,
    sizes: {
        xs: 16,
        s: 20,
        base: 30,
        m: 36,
        l: 42,
        xl: 50,
        xxl: 58,
    },
    color: palette
};

export default theme;
